# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
sampledump/
node_modules/
dist/

# Environment / secrets — never commit
.env
.env.local
.env.*
*.key

# Pi server — digest captures (never commit capture data)
pi/digests/*.json

# Capture JSON files (except package files)
*.json
!package.json
!package-lock.json
